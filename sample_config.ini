[PATHS]
model_path_severson =  path where LSTM models only trained on severson are saved (with variance)
model_path_dnn =  path where DNN models only trained on severson are saved (with variance)
figure_path = where to save figures
result_path = where to save result files (i.e. pkls fir the step between calculating and visualization)
[DATASET]
severson_path = data path to the severson pkl's

